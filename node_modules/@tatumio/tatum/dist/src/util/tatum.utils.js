"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.TatumUtils = void 0;
const constant_1 = require("./constant");
exports.TatumUtils = {
    getChainId: (network) => {
        if (network in constant_1.Constant.NETWORK.ChainId) {
            return constant_1.Constant.NETWORK.ChainId[network];
        }
        throw new Error(`Tatum Network to ChainId for network ${network} does not exist`);
    },
    getNetwork: (chainId) => {
        if (Object.keys(chainIdToNetworkCache).length === 0) {
            buildChainIdToNetworkCache();
        }
        const network = chainIdToNetworkCache[chainId];
        if (!network) {
            throw new Error(`ChainId to Tatum Network for chainId ${chainId} does not exist`);
        }
        return network;
    },
};
const chainIdToNetworkCache = {};
const buildChainIdToNetworkCache = () => {
    for (const network of Object.keys(constant_1.Constant.NETWORK.ChainId)) {
        const netKey = network;
        chainIdToNetworkCache[constant_1.Constant.NETWORK.ChainId[netKey]] = netKey;
    }
};
//# sourceMappingURL=tatum.utils.js.map