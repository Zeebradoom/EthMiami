import { Network } from '../../../dto';
import { BaseEvm } from '../../../service';
interface evmE2eI {
    network: Network;
    apiKey?: string;
    expected: {
        chainId: number;
    };
    data?: {
        estimateGas?: any;
    };
    skipEstimateGas?: boolean;
}
export declare const EvmE2eUtils: {
    initTatum: <T extends BaseEvm>(network: Network, apiKey?: string) => Promise<T>;
    e2e: (evmE2eI: evmE2eI) => void;
};
export {};
